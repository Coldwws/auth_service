version: "3.9"

volumes:
  postgres_volume_local:

services:
  pg_local:
    image: postgres:14-alpine3.17
    env_file:
      - docker.env
    ports:
      - "5438:5432"
    volumes:
      - "postgres_volume_local:/var/lib/postgresql/data"